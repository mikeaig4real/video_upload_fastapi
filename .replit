run = "uvicorn main:app --host 0.0.0.0 --port 8080"
modules = ["python-3.10"]

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port 8080"]

[nix]
channel = "stable-25_05"
packages = ["cacert", "cargo", "glibcLocales", "libiconv", "libxcrypt", "postgresql", "rustc"]
